# Define your 5v5 team drafts
blue_picks <- c(top="KSante", jng="Wukong", mid="Leblanc", adc="Senna", sup="Nautilus")
red_picks  <- c(top="Gragas", jng="JarvanIV", mid="Cassiopeia", adc="Corki", sup="Rell")

# Run it!
simulate_final_draft(blue_picks, red_picks)

baseline_prob


base_rate_leader <- mean(d_train$leader_won_drag == "Leader_Drag")

names(probabilities)

sort(unique(champion_profiles$championName))
